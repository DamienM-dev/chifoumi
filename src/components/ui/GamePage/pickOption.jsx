import React from "react";

const pick = "Pick an option:";

export default function PickOption() {
  return (
    <div className="mt-16 text-center">
      <p className="uppercase text-lg">{pick}</p>
    </div>
  );
}
